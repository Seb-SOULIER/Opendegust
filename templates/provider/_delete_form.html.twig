<form method="post" action="{{ path('provider_delete', {'id': provider.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer votre compte?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ provider.id) }}">
    <button class="btn text-danger">Supprimer mon compte</button>
</form>
