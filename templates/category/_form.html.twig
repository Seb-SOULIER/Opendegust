{{ form_start(form, {
    'attr': {
        'class': 'mb-4'
    }
}) }}
    {{ form_widget(form) }}
<button class="btn btn-success"><i class="fas fa-save"></i> {{ button_label|default('Save') }}</button>
{{ form_end(form) }}
