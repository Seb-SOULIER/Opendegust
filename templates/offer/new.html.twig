{% extends 'base.html.twig' %}

{% block title %}Nouvelle offre{% endblock %}

{% block body %}
    <div class="d-flex justify-content-center">
        <h1>Créer une nouvelle offre</h1>
    </div>

        {{ include('offer/_form_new_2.html.twig') }}

    <div class="d-flex justify-content-center">
        <a href="{{ path('offer_index') }}">Retour a mes offres</a>
    </div>

{% endblock %}
