{% extends 'base.html.twig' %}

{% block title %}Edit Offer{% endblock %}

{% block body %}
<div class="container-fluid">
<div class="w-25">
<a class="btn btn-primary m-3" href="{{ path('offer_index') }}">Retour</a>
</div>
    </div>
    {{ include('offer/_form_new.html.twig', {'button_label': 'Update'}) }}
    {{ include('offer/_delete_form.html.twig') }}
{% endblock %}
